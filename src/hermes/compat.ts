/** Compatibility re-exports for older Hermes-named imports. */
export { RuntimeClient as HermesRuntimeClient, RuntimeClient } from "../agent/client.js";
export { ToolBridge as HermesToolBridge, ToolBridge, createDefaultBridge } from "../agent/bridge.js";
export { AGENT_TOOLS as HERMES_TOOLS, AGENT_TOOLS } from "../agent/tools.js";
